.tabs-content
    .post(ng-repeat='thread in userProfile.user.threads')
        // Post
        .post.clearfix
            .user-block
                img.img-circle.img-bordered-sm(check-image='', ng-src='images/user7-128x128.jpg', alt='user image')
                span.username
                    a(href='#') Sarah Ross
                    a.pull-right.btn-box-tool(href='#')
                        i.fa.fa-times
                span.description Sent you a message - 3 days ago
            // /.user-block
            p
                | Lorem ipsum represents a long-held tradition for designers,
                | typographers and the like. Some people hate it and argue for
                | its demise, but others ignore the hate as they create awesome
                | tools to help create filler text for everyone from bacon lovers
                | to Charlie Sheen fans.
            form.form-horizontal
                .form-group.margin-bottom-none
                    .col-sm-9
                        input.form-control.input-sm(placeholder='Response')
                    .col-sm-3
                        button.btn.btn-danger.pull-right.btn-block.btn-sm Send
        // /.post
    .new-post
        form(method='post', ng-submit='addMessage(message)', name='messageForm', novalidate)
            .form-group.has-feedback(ng-class="{ 'has-error' : message.subject.$invalid && !messageForm.subject.$pristine }")
                input.form-control.input-lg(type='text', name='subject', ng-model='message.subject', placeholder='Subject', required='', autofocus='')
                p.help-block(ng-show='message.subject.$invalid && !messageForm.subject.$pristine') Subject is required.
            .form-group.has-feedback(ng-class="{ 'has-error' : message.message.$invalid && !messageForm.message.$pristine }")
                textarea.form-control(type='text', name='message', ng-model='message.message', placeholder='Message', required='')
                p.help-block(ng-show='message.message.$invalid && !messageForm.message.$pristine') Message is required.
            .row
                .col-sm-6
                    .form-group.has-feedback
                        label.control-label
                            i.ion-person
                            |  Friends
                        br
                        multiselect(ng-model='friends', options='f.displayName for f in friends', data-multiple='true', scroll-after-rows='5', data-compare-by='id')
                .col-sm-6
                    .form-group.has-feedback
                        label.control-label
                            i.ion-person
                            |  Followers
                        br
                        multiselect(ng-model='followers', options='f.displayName for f in followers', data-multiple='true', scroll-after-rows='5', data-compare-by='id')
            .form-group
                button.btn.btn-primary(type='submit', ng-disabled='messageForm.$invalid') Send
